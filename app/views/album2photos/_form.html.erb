<%= form_for(@album2photo) do |f| %>
  <% if @album2photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album2photo.errors.count, "error") %> prohibited this album2photo from being saved:</h2>

      <ul>
      <% @album2photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :album_id %><br />
    <%= f.number_field :album_id %>
  </div>
  <div class="field">
    <%= f.label :published_at %><br />
    <%= f.datetime_select :published_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
